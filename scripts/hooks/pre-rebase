#!/bin/bash
# Backup .dev/ directory before rebase on dev branch

BRANCH=$(git rev-parse --abbrev-ref HEAD)

if [ "$BRANCH" = "dev" ] && [ -d ".dev" ]; then
    rm -rf .git/.dev-backup
    cp -r .dev .git/.dev-backup
    echo "📦 Backed up .dev/ before rebase"
fi
